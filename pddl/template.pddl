(define
    (domain hanoi)
    (:requirements :adl)
    (:types )
    (:constants )
    (:predicates
    )
    (:action nazwa
        :parameters ()
        :precondition
        (and
        )
        :effect
        (and
        )
    )
)
